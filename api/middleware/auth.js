/**
 *  Authorization using jsonwebtoken
 *  Inorder to protect the router,
 *  when user call the router, client side
 *  need to send the token to verify the user.
 */
const jwt = require("jsonwebtoken");

module.exports = (req, res, next) => {};
